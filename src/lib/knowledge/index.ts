/** @deprecated Prefer `@/lib/templates` — kept for curated seed reference. */
export { INDUSTRY_TEMPLATES, TEMPLATE_BY_ID } from "./templates";
export { retrieveTemplates, formatTemplatesForPrompt } from "./retrieve";
export type {
  IndustryTemplate,
  TemplateMatch,
  RetrievedTemplates,
} from "./types";
